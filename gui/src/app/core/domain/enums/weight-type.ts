export enum WeightType {
    FROM_SCRATCH = 'from_scratch',
    PRE_TRAINED = 'pre_trained',
    PRE_TRAINED_OFFILE = 'pre_trained_offline',
    PRE_TRAINED_CLASSIFIER = 'pre_trained_classifier',
    FROM_CHECKPOINT = 'from_checkpoint'
}
